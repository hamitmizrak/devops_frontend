version: '3'

services:
  frontend:
    container_name: blog_frontend
    image: blog_frontend
    restart: always
    build: ./
    ports: 
      - "3000:3000"
    networks:
      - springmysql 

networks:
  springmysql:
    name: springmysql
    driver: bridge     